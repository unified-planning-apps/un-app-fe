/**
 * LocaleSwitcher — single-language build (English only).
 * The switcher is hidden since there is only one locale.
 * Keeping the component so the import in _admin.tsx stays valid.
 */
export default function ParaglideLocaleSwitcher() {
  // Only one locale configured — nothing to switch, render nothing.
  return null
}
